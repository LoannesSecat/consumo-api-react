const initialState = {};

export default function UserReducer(store = initialState, action) {
  switch (action.type) {
    case "a":
      break;
    case "b":
      break;

    default:
      return store;
  }
}
